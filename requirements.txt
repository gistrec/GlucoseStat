# Core
# LibreLinkUp — свой клиент в librelinkup.py: pylibrelinkup падает на
# TrendArrow=0, который Abbott шлёт весь прогрев нового сенсора.
requests
python-dotenv
# База часовых поясов для ZoneInfo: slim-образы системной не несут, и без неё
# публикация падала бы на первом же обращении к зоне. Fallback на UTC не
# заводим: тихо съехавшие на два часа сутки хуже отказа.
tzdata

# Database
sqlalchemy>=2.0
# PyMySQL, а не mysqlclient: чистый Python, не требует libmysqlclient-dev
PyMySQL
